<Window x:Class="AwakeDesk.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AwakeDesk"
        mc:Ignorable="d"
        Height="18"
        Width="53"
        ResizeMode="NoResize" 
        WindowStyle="none" 
        AllowsTransparency="False"
        Topmost="True"
        Icon="../Media/ico/Clock.ico"
        ShowInTaskbar="False"
        Title="Awake Desk" >
    <Window.Background>
        <ImageBrush ImageSource="../Media/img/WinTaskBar.png" Stretch="UniformToFill" />
    </Window.Background>
    <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0" Name="ClockPanel" HorizontalAlignment="Right">
                <TextBlock 
                        x:Name="ClockTextBlock" 
                        Text="{Binding ActualTime}" 
                        FontSize="13"
                        Margin="0" 
                        Padding="0" 
                        VerticalAlignment="Top" 
                        HorizontalAlignment="Right"
                        Height="40"
                        />
            </StackPanel>
        </StackPanel>
</Window>
